# port 端口号
VITE_PORT=8080

# api base url 后端接口地址
VITE_API_URL=http://10.10.0.18:8000/

# open 运行 npm run dev 时自动打开浏览器（true/false）
VITE_OPEN=false

# public path 配置线上环境路径（打包）
# 本地通过 http-server 访问时，请置空即可（VITE_PUBLIC_PATH=）
VITE_PUBLIC_PATH=/

# dist 输出目录
VITE_DIST_PATH=dist